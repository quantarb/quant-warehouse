name: quant-warehouse
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - numpy>=1.26,<2.5
  - pandas>=2.1,<3
  - pytest>=8.0
  - ruff>=0.6
  - pyarrow
  - arcticdb>=6.18
  - protobuf<7
  - pip:
      - -e ".[openbb,dev]"
