name: quant-warehouse
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - numpy>=1.26
  - pandas>=2.1
  - pytest>=8.0
  - ruff>=0.6
  - pyarrow
  - arcticdb>=6.18
  - pip:
      - -e ".[openbb,dev]"